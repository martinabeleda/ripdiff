`src/difftastic` and `src/vendored_parsers` are vendored from the difftastic fork at:

- https://github.com/seatedro/difftastic

The full fork is also tracked as a git submodule at `vendor/difftastic` for easier syncing and provenance.
