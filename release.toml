allow-branch = ["main"]
consolidate-commits = true

tag-name = "v{{version}}"
tag-message = "ripdiff {{version}}"

pre-release-commit-message = "release"
push-remote = "origin"

publish = false
push = true
tag = true
